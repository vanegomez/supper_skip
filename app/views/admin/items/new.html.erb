<%= render :partial => "shared/individual_header" %>
<h1>New Pie</h1>

<div class="content-wrapper formStyle">
  <%= render 'form' %>

  <%= link_to 'Back to All Items', admin_restaurant_item_path(restaurant_slug: @restaurant.slug) %>
</div>
