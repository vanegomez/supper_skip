<%= render :partial => "shared/individual_header" %>
<h1>Editing: <%= @category.name %></h1>

<div class="content-wrapper formStyle">
  <%= render 'form' %>

  <%= link_to 'Back', admin_restaurant_categories_path(id: @category.id, restaurant_slug: @restaurant.slug) %>
</div>
